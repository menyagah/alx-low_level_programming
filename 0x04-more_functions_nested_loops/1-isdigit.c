#include "main.h"

/**
 * _isdigit - prints whether c is a digit
 * @c : interger to check
 * Return: zero
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
return (0);
}
