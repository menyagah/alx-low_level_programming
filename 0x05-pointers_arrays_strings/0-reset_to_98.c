#include <main.h>

/**
 * main - start of the function
 *@n: the parameter to be passed
 *
 * Return: returns 0
 */

int reset_to_98(int *n)
{
	n = 98;
	printf("value of 'n': %d\n", n);
	return (0);
}
